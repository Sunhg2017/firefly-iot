# 视频设备使用说明

## 1. 入口

- 从租户空间进入 `设备资产 -> 设备管理`
- 切到 `视频设备` 视图
- 或从摄像头产品页点击 `视频接入` 直接跳转进入

## 2. 从产品页进入时会发生什么

1. 页面自动切到视频设备视图。
2. 自动打开新增视频设备抽屉。
3. 自动带入当前产品。
4. 自动锁定与产品一致的协议。

如果你不是从产品页进入，也可以手动在抽屉里选择摄像头产品。

## 3. 新建视频设备

1. 点击 `新增视频设备`
2. 选择摄像头产品
3. 填写设备名称
4. 按协议填写接入参数
5. 点击保存

### 3.1 GB28181

必填项：

- `GB 设备编号`
- `GB 域`
- `传输方式`
- `设备级 SIP 密码`（必填，平台强制 Digest 鉴权）

播放说明：

- 点击播放后，平台会先向 ZLMediaKit 打开专用 RTP 收流口，再向设备发送 INVITE。
- 如媒体服务返回的 RTP 端口字段位置或类型存在差异，平台会自动兼容顶层 `port`、`data.port` 以及字符串端口。
- GB28181 流在平台内统一挂到 `rtp/{streamId}`，因此网页播放、截图、录像都基于这一路流。

### 3.2 RTSP / RTMP

必填项：

- `视频源地址`

建议直接填写完整地址，例如：

- `rtsp://192.168.1.10:554/live/cam01`
- `rtmp://192.168.1.10/live/cam01`

## 4. 列表与查询

- 首次进入页面会自动查询一次
- 修改筛选条件后不会自动刷新
- 需要点击 `查询` 按钮才会重新加载

## 5. 通道与媒体控制

在视频设备列表中可以执行：

- 查看通道
- 播放
- 停止
- 截图
- 目录查询
- 设备信息查询
- 云台控制
- 开始录像
- 停止录像

所有媒体控制都按平台 `deviceId` 调用。

## 6. 常见问题

### 6.1 为什么同一台设备不能重复创建

平台会按接入标识做唯一校验：

- `GB28181` 用 `gbDeviceId`
- `RTSP / RTMP` 用完整 `sourceUrl`

### 6.2 为什么保存成功后列表里没有

先检查：

- 设备资产是否创建成功
- 当前账号是否有该设备的数据权限
- 是否误切到了普通设备视图

### 6.3 为什么 SIP 密码正确但还是认证失败

先检查：

- 设备级 `sip_password` 是否已保存
- `gbDeviceId` 和 `gbDomain` 是否与设备端一致
- 设备端是否使用同一密码参与 Digest 认证

### 6.4 为什么页面里找不到旧的视频监控菜单

因为视频设备已经并入 `设备管理`，不再保留独立 `视频监控` 页面和菜单。

### 6.5 播放提示网络错误怎么办

先检查：

- 平台返回的播放地址是否还是 `localhost`
- 运维是否已把 `zlmediakit.public-host/public-port/public-scheme` 配成浏览器可访问地址
- 设备是否在线并且已成功开流

### 6.6 云台控制提示参数错误怎么办

先检查：

- 当前设备是否为 `GB28181`
- `command` 是否为合法 PTZ 指令（数字或枚举名）
- 速度参数是否为合理范围
