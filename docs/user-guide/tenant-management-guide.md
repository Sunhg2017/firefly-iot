# 租户管理使用说明

## 1. 适用角色

- 系统运维
- 平台管理员

## 2. Webhook 入口

在“租户管理”列表中，每个租户行都提供 `Webhook` 操作入口。

## 3. 如何维护租户 Webhook

1. 打开“租户管理”
2. 找到目标租户
3. 点击该行的 `Webhook`
4. 在抽屉中新增或编辑 Webhook
5. 填写地址、请求方法、Content-Type 和可选签名密钥
6. 保存后可直接使用“测试”校验连通性

## 4. 注意事项

- 这里只维护当前租户自己的 Webhook
- Webhook 不会出现在平台默认通知渠道页面
- 若租户没有配置 Webhook，通知发送不会自动回落到平台 Webhook
