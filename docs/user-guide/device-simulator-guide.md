# 设备模拟器使用说明

## 1. 新建 Video 设备

1. 点击 `新建`
2. 选择 `Video`
3. 选择摄像头产品或填写 `ProductKey`
4. 选择 `GB28181 / RTSP / RTMP`
5. 补充对应参数

## 2. Video 模式怎么连接

点击 `连接` 后，模拟器会先同步平台设备资产，再执行媒体控制。

同步顺序：

1. 通过 `DEVICE` 路由查重
2. 创建或更新视频设备资产
3. 保存 `platformDeviceId`
4. 后续控制都使用这个 `platformDeviceId`

Video 设备顶部状态卡口径：

1. `GB28181`：显示 `SIP 注册` 与 `SIP 心跳`
2. `RTSP / RTMP`：显示 `平台同步` 与 `连接状态`

## 3. GB28181 怎么调

1. 填写 `GB 设备编号`
2. 填写 `GB 域`
3. 填写设备级 `SIP 密码`（必填）
4. 连接后执行 `SIP 注册`
5. 需要时再执行 `开启心跳`

## 4. RTSP / RTMP 怎么调

1. 选择 `RTSP` 或 `RTMP`
2. 填完整 `sourceUrl`
3. 点击连接
4. 连接成功后可执行播放、截图、录像

## 5. 常见问题

### 5.1 为什么第二次连接没有再创建新设备

因为模拟器会优先复用同一台设备的 `platformDeviceId`。

### 5.2 为什么通道列表现在不走 MEDIA

因为通道是设备资产主数据，已经改为走 `DEVICE` 路由。

### 5.3 为什么页面里不再显示“平台视频设备”

因为现在绑定的是统一设备资产，所以口径统一改成 `平台设备资产`。

### 5.4 为什么 GB28181 已注册但云端播放还是失败

当前模拟器的 `GB28181` 仅模拟 SIP 信令（REGISTER / MESSAGE / INVITE / BYE / PTZ），不包含本地摄像头采集与 RTP 码流发送能力。  
如果要验证“摄像头推流到云端再播放”，请使用：

1. `RTSP / RTMP` 模式并填写真实 `sourceUrl`
2. 或真实 GB28181 设备推流
