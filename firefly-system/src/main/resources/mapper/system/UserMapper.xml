<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.songhg.firefly.iot.system.mapper.UserMapper">

    <select id="findByIdentifierGlobal" resultType="com.songhg.firefly.iot.system.entity.User">
        SELECT id,
               tenant_id,
               username,
               password_hash,
               phone,
               email,
               avatar_url,
               real_name,
               user_type,
               status,
               login_fail_count,
               deleted_at
        FROM users
        WHERE (username = #{identifier} OR phone = #{identifier} OR email = #{identifier})
          AND deleted_at IS NULL
        ORDER BY id ASC
    </select>

</mapper>
